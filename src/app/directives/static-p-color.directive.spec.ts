import { StaticPColorDirective } from './static-p-color.directive';

describe('StaticPColorDirective', () => {
  it('should create an instance', () => {
    const directive = new StaticPColorDirective();
    expect(directive).toBeTruthy();
  });
});
